{
  "name": "nathan-james-api-postgresql",
  "version": "1.0.0",
  "private": true,
  "description": "Books API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "start:dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
